import FavoritScreen from '@/components/main/favorit-screen';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Favorit() {
    return (
        <SafeAreaView className='flex-1  bg-white'>
            <FavoritScreen />
        </SafeAreaView>
    )
};

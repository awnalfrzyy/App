import ProfilScreen from "@/components/main/profil-screen";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Profil() {
    return (
        <SafeAreaView className="flex-1 bg-white">
            <ProfilScreen />
        </SafeAreaView>
    )
};
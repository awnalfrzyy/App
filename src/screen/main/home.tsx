import HomeScreen from "@/components/main/home-screen";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Homes() {
    return (
        <SafeAreaView className="flex-1">
            <HomeScreen />
        </SafeAreaView>
    )
};
